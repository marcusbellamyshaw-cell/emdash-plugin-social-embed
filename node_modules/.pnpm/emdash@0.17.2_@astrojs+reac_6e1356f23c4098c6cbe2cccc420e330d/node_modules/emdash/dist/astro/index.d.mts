import { r as ContentItem } from "../types-i8_uzhMD.mjs";
import "../options-tb7DJROi.mjs";
import "../types-DawhLFwy.mjs";
import { yt as ResolvedPlugin } from "../types-DMwSpvcw.mjs";
import "../byline-fields-CR5hGLMw.mjs";
import { Bt as S3StorageConfig, It as EmDashConfig, Jr as MediaItem, Rt as getStoredConfig, Vt as StorageDescriptor, zt as LocalStorageConfig } from "../index-C8ciqSMJ.mjs";
import "../runner-DM1yR5qd.mjs";
import "../index-D60_SzHG.mjs";
import "../types-DWnN7weG.mjs";
import "../validate-Dy6nkNls.mjs";
import { EmDashHandlers, EmDashManifest, ManifestCollection } from "./types.mjs";
import { AstroIntegration } from "astro";

//#region src/astro/storage/adapters.d.ts
/**
 * S3-compatible storage adapter
 *
 * Works with AWS S3, Cloudflare R2 (via S3 API), MinIO, etc.
 *
 * Any field omitted here is resolved from the matching `S3_*` environment
 * variable when the container starts (`S3_ENDPOINT`, `S3_BUCKET`,
 * `S3_ACCESS_KEY_ID`, `S3_SECRET_ACCESS_KEY`, `S3_REGION`, `S3_PUBLIC_URL`).
 * Explicit values always take precedence over env vars.
 *
 * Note: env var resolution reads `process.env` on Node at runtime.
 * Workers users should continue passing explicit values to `s3({...})`.
 *
 * @example
 * ```ts
 * // All fields from env (container deployments)
 * storage: s3()
 *
 * // Mix: CDN from config, credentials from env
 * storage: s3({ publicUrl: "https://cdn.example.com" })
 *
 * // All explicit (unchanged from before)
 * storage: s3({
 *   endpoint: "https://xxx.r2.cloudflarestorage.com",
 *   bucket: "media",
 *   accessKeyId: process.env.R2_ACCESS_KEY_ID,
 *   secretAccessKey: process.env.R2_SECRET_ACCESS_KEY,
 * })
 * ```
 */
declare function s3(config?: Partial<S3StorageConfig>): StorageDescriptor;
/**
 * Local filesystem storage adapter
 *
 * For development and testing. Stores files in a local directory.
 * Does NOT support signed upload URLs.
 *
 * @example
 * ```ts
 * storage: local({
 *   directory: "./uploads",
 *   baseUrl: "/_emdash/api/media/file",
 * })
 * ```
 */
declare function local(config: LocalStorageConfig): StorageDescriptor;
//#endregion
//#region src/astro/integration/index.d.ts
/**
 * Create the EmDash Astro integration
 */
declare function emdash(config?: EmDashConfig): AstroIntegration;
//#endregion
export { type ContentItem, type EmDashConfig, type EmDashHandlers, type EmDashManifest, type LocalStorageConfig, type ManifestCollection, type MediaItem, type ResolvedPlugin, type S3StorageConfig, type StorageDescriptor, emdash as default, getStoredConfig, local, s3 };
//# sourceMappingURL=index.d.mts.map