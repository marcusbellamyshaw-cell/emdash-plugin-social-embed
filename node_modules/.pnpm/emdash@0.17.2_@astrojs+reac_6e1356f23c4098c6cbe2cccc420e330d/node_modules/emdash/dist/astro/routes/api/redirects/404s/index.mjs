import "../../../../../dialect-helpers-DRI5pyY3.mjs";
import "../../../../../base64-CqR-7kqF.mjs";
import "../../../../../types-CfyYQ7eY.mjs";
import "../../../../../options-BL4X94qY.mjs";
import "../../../../../redirect-C-FeA4j9.mjs";
import { a as unwrapResult, i as requireDb, r as handleError } from "../../../../../error-Bk9s3Ism.mjs";
import { i as parseQuery, n as parseBody, t as isParseError } from "../../../../../parse-B-K21lvm.mjs";
import { a as notFoundPruneBody, r as notFoundListQuery } from "../../../../../redirects-C0L9JUk4.mjs";
import "../../../../../byline-fields-Dr-xcb6S.mjs";
import "../../../../../api/schemas/index.mjs";
import { n as requirePerm } from "../../../../../authorize-CLTmOUyx.mjs";
import { n as handleNotFoundList, r as handleNotFoundPrune, t as handleNotFoundClear } from "../../../../../redirects-C1UgU9E0.mjs";

//#region src/astro/routes/api/redirects/404s/index.ts
const prerender = false;
const GET = async ({ url, locals }) => {
	const { emdash, user } = locals;
	const dbErr = requireDb(emdash?.db);
	if (dbErr) return dbErr;
	const db = emdash.db;
	const denied = requirePerm(user, "redirects:read");
	if (denied) return denied;
	try {
		const query = parseQuery(url, notFoundListQuery);
		if (isParseError(query)) return query;
		return unwrapResult(await handleNotFoundList(db, query));
	} catch (error) {
		return handleError(error, "Failed to fetch 404 log", "NOT_FOUND_LIST_ERROR");
	}
};
const DELETE = async ({ locals }) => {
	const { emdash, user } = locals;
	const dbErr = requireDb(emdash?.db);
	if (dbErr) return dbErr;
	const db = emdash.db;
	const denied = requirePerm(user, "redirects:manage");
	if (denied) return denied;
	try {
		return unwrapResult(await handleNotFoundClear(db));
	} catch (error) {
		return handleError(error, "Failed to clear 404 log", "NOT_FOUND_CLEAR_ERROR");
	}
};
const POST = async ({ request, locals }) => {
	const { emdash, user } = locals;
	const dbErr = requireDb(emdash?.db);
	if (dbErr) return dbErr;
	const db = emdash.db;
	const denied = requirePerm(user, "redirects:manage");
	if (denied) return denied;
	try {
		const body = await parseBody(request, notFoundPruneBody);
		if (isParseError(body)) return body;
		return unwrapResult(await handleNotFoundPrune(db, body.olderThan));
	} catch (error) {
		return handleError(error, "Failed to prune 404 log", "NOT_FOUND_PRUNE_ERROR");
	}
};

//#endregion
export { DELETE, GET, POST, prerender };
//# sourceMappingURL=index.mjs.map