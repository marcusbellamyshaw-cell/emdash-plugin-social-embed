import "../../../../../base64-CqR-7kqF.mjs";
import "../../../../../types-CfyYQ7eY.mjs";
import "../../../../../comment-DkAfGX9E.mjs";
import { a as unwrapResult, i as requireDb, r as handleError } from "../../../../../error-Bk9s3Ism.mjs";
import { n as parseBody, t as isParseError } from "../../../../../parse-B-K21lvm.mjs";
import { pt as commentBulkBody } from "../../../../../redirects-C0L9JUk4.mjs";
import "../../../../../byline-fields-Dr-xcb6S.mjs";
import "../../../../../api/schemas/index.mjs";
import { n as requirePerm } from "../../../../../authorize-CLTmOUyx.mjs";
import { n as handleCommentBulk } from "../../../../../comments-DLFnXs7J.mjs";

//#region src/astro/routes/api/admin/comments/bulk.ts
const prerender = false;
const POST = async ({ request, locals }) => {
	const { emdash, user } = locals;
	const dbErr = requireDb(emdash?.db);
	if (dbErr) return dbErr;
	try {
		const body = await parseBody(request, commentBulkBody);
		if (isParseError(body)) return body;
		if (body.action === "delete") {
			const denied = requirePerm(user, "comments:delete");
			if (denied) return denied;
		} else {
			const denied = requirePerm(user, "comments:moderate");
			if (denied) return denied;
		}
		return unwrapResult(await handleCommentBulk(emdash.db, body.ids, body.action));
	} catch (error) {
		return handleError(error, "Failed to perform bulk operation", "COMMENT_BULK_ERROR");
	}
};

//#endregion
export { POST, prerender };
//# sourceMappingURL=bulk.mjs.map