import { i as PublisherSession, n as Did, t as CredentialStore } from "./types-DNGNVV4Q.js";

//#region src/credentials/env.d.ts
interface EnvCredentialStoreOptions {
  /**
   * Override the env-var source. Defaults to `process.env`. Mainly useful for
   * tests; production callers should leave it alone.
   */
  env?: Record<string, string | undefined>;
}
declare class EnvCredentialStore implements CredentialStore {
  #private;
  constructor(options?: EnvCredentialStoreOptions);
  current(): Promise<PublisherSession | null>;
  get(did: Did): Promise<PublisherSession | null>;
  list(): Promise<PublisherSession[]>;
  put(): Promise<void>;
  setCurrent(): Promise<void>;
  remove(): Promise<void>;
}
//#endregion
//#region src/credentials/file.d.ts
interface FileCredentialStoreOptions {
  /**
   * Path to the credentials file. Defaults to `~/.emdash/credentials.json`.
   * Tests typically pass a temp-dir path here.
   */
  path?: string;
}
declare class FileCredentialStore implements CredentialStore {
  #private;
  readonly path: string;
  constructor(options?: FileCredentialStoreOptions);
  current(): Promise<PublisherSession | null>;
  get(did: Did): Promise<PublisherSession | null>;
  list(): Promise<PublisherSession[]>;
  put(session: PublisherSession): Promise<void>;
  setCurrent(did: Did): Promise<void>;
  remove(did: Did): Promise<void>;
}
//#endregion
//#region src/credentials/memory.d.ts
declare class MemoryCredentialStore implements CredentialStore {
  #private;
  current(): Promise<PublisherSession | null>;
  get(did: Did): Promise<PublisherSession | null>;
  list(): Promise<PublisherSession[]>;
  put(session: PublisherSession): Promise<void>;
  setCurrent(did: Did): Promise<void>;
  remove(did: Did): Promise<void>;
}
//#endregion
export { EnvCredentialStore as i, FileCredentialStore as n, FileCredentialStoreOptions as r, MemoryCredentialStore as t };
//# sourceMappingURL=memory-Ci3gbSC-.d.ts.map